# A simple extension in Python 3

This extension uses *multi-phase initialization* described in [PEP 489][pep].

Build via `make`:

```sh
$ make
```

Build manually:

```sh
$ python setup.py build
$ python setup.py install
```

[pep]: https://www.python.org/dev/peps/pep-0489/
